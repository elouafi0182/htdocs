{% extends 'base.html.twig' %}

{% block title %}Betaal{% endblock %}

{% block body %}
<div class="container">
    <h1>Betaal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ betaal.id }}</td>
            </tr>
            <tr>
                <th>Useid</th>
                <td>{{ betaal.useid }}</td>
            </tr>
            <tr>
                <th>Soort</th>
                <td>{{ betaal.soort }}</td>
            </tr>
            <tr>
                <th>Creditcardnr</th>
                <td>{{ betaal.creditcardnr }}</td>
            </tr>
            <tr>
                <th>Betaaldate</th>
                <td>{{ betaal.betaaldate ? betaal.betaaldate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('betaal_index') }}">back to list</a>

    <a href="{{ path('betaal_edit', {'id': betaal.id}) }}">edit</a>

    {{ include('betaal/_delete_form.html.twig') }}
    </div>
{% endblock %}
